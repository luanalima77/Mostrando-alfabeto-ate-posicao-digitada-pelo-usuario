# Mostrando o alfabeto até a posição digitada pelo usuário
Este repositório contém um exercício prático em Python cujo objetivo é retornar um array com as letras do alfabeto até a posição informada pelo usuário, aplicando validação de entrada e slicing.

## 🎯 Regras do projeto
- Receber um número inteiro informado pelo usuário;
- Aceitar apenas valores de 1 a 26;
- Retornar um array com as letras do alfabeto até a posição indicada;
- Interromper a execução corretamente em casos inválidos.

Exemplo de funcionamento:
```python
Entrada: 8

Saída: ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H']
```

## 🧠 Conceitos trabalhados
- Listas em Python;
- Fatiamento de listas (slice);
- Validação de dados de entrada;
- Fluxo de execução com return.


## ▶️ Como executar este código no seu computador?
Siga os passos abaixo:

### 1) Pré-requisitos
- Ter o Python 3 instalado;
- Ter o VS Code instalado;
- Ter o Git instalado;
- Verificar a instalação do Python no terminal do VS Code com o seguinte comando:

```bash
python --version
```
<br>

### 2) Clonar o repositório
- No terminal do VS Code, execute o seguinte comando:
```bash
git clone https://github.com/luanalima77/Mostrando-alfabeto-ate-posicao-digitada-pelo-usuario.git
```
<br>

- Entre na pasta do projeto por meio do seguinte comando no terminal do VS Code:
```bash
cd Mostrando-alfabeto-ate-posicao-digitada-pelo-usuario
```
<br>

### 3) Executar o código
- No terminal do VS Code, insira o seguinte comando para rodar o código:
```bash
python main.py
```
<br>

### 4) Interagir no terminal
- Digite um número entre 1 e 26 quando solicitado.

Exemplo:
```bash
Digite um número de 1 a 26: 8
['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H']
```
<br>

## 👩‍💻 Autora
**Luana de Jesus Lima**  
[Meu LinkedIn](https://www.linkedin.com/in/luana-de-jesus-lima)  

### ✍️ Projeto com fins educacionais